#include "main.h"
/**
 * add -> adds two number
 * @x : param1
 * @y : param2
 * Return: x + y
 */
int add(int x, int y)
{
	int x;
	int y;
	int res;

	res = add(x, y);
		_putchar(res);
return (res);
}
